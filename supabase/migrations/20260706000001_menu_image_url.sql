-- ============================================================================
-- メニューに画像 URL を追加。
-- 予約フォームのメニューカードや公開メニューページで写真を表示できるようにする。
-- 画像そのものは外部（public/images/... や画像 CDN）に置き、URL を保持する方式。
-- ============================================================================
alter table public.menus
  add column if not exists image_url text;
